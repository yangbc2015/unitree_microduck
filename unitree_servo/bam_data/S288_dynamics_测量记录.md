# S288 动力学测量记录（M2 / M6b / M7）

标定日期 2026-09-11 ｜ S288 ID 0 ｜ 12.0 V ｜ 空载自由轴、**机身在台面上未夹持**
舵机壳体 55–60 ℃ ｜ 主机 Linux 6.8 aarch64 ｜ 环路实测 6.1–6.4 kHz（每帧往返 0.16 ms）

工具：`bam_dynamics.py`（`delay` / `compliance` / `backlash` / `inertia` / `sine`），
原始逐帧数据在 `bam_data/M2_*`、`M7_*`、`M6b_*`（未压缩 CSV 已 gzip）。
摩擦模型（`bam_fit.py`）是这次惯量测量的前提：`J = (τ − F(v)) / α`，F(v) 已知。

---

## 一、M2 指令延迟：0.48 ms 传输 + 一阶力矩建立 τ=0.54 ms

方法：`kp=kd=0`，让**纯力矩前馈**在 +0.02 / −0.02 N·m 之间跳变。两个值都**低于破断力矩
0.0365 N·m，所以轴完全不动** —— 这样测到的纯粹是"命令到达并体现在舵机自己的力矩读数上"
的时间，不含任何机械响应。80 次跃变（40 轮 × 双向）全部原始点一起拟合：

```
y(t) = 0                      t < td
y(t) = A·(1 − exp(−(t−td)/τ))  t ≥ td
```

| 量 | 值 | 说明 |
|----|-----|------|
| 传输延迟 `td` | **0.48 ms** | 拟合最优值；SSE 在 10% 以内的区间 0.44–0.51 ms |
| 力矩建立时间常数 `τ` | **0.54 ms** | 一阶；对应 10–90% 上升 **1.19 ms** |
| 幅度 A | 37.55 counts = **42.3 mN·m** | 命令 40 mN·m，偏高 5.8%（与已知的标度偏置一致） |
| 拟合残差 rms | 2.26 counts | 反馈噪声约 1 count → 一阶模型成立 |
| 逐次"首次偏离" | 0.34 / 0.58 / 3.90 ms（min/中位/max） | 受主机调度抖动影响，故以拟合值为准 |

要点：

* **延迟和上升要分开报**。手册预期"S288 延迟 < 5 ms"，实测端到端只有 0.48 ms，而且其中
  一大部分是 USB-CDC + 主机调度 —— 换成 robotd 直接走 Jetson UART（6 Mbps）时会变。
* 反馈的力矩读数在 40 mN·m 阶跃上**跟随命令到 1.7 mN·m 以内**（另一次测量），
  所以"命令值 ≈ 实际值"这个假设在这次测量里是成立的。
* 一阶上升意味着 sim 里的执行器模型不能只加纯延迟，还需要一个 ~0.5 ms 的一阶惯性。

---

## 二、M6b 传动链柔度：极硬，不是编码器偏差的来源

方法：`kp=kd=0`，力矩从 0 → ±0.032 N·m 阶梯扫描（全部低于破断点，轴不转），
同时看两个编码器：转子端推出的输出角（`pos/RATIO`）与输出端 13 bit 编码器（`OutPos`）。

| τ_cmd | 转子端推出的输出角变化 | 输出端编码器变化 | 差值 |
|-------|----------------------|----------------|------|
| ≤ 0.024 N·m | ≤ 1.5e-5 rad | **< 1 count**（分辨率 0.044°） | 即测不出 |
| 0.028 N·m | +6.4e-4 rad | +7.7e-4 rad | 开始蠕动 |
| 0.032 N·m | +1.8e-3 rad | +3.9e-3 rad | 明显蠕动（0.004 rad / 0.25 s） |

结论：

1. **传动链刚度 ≥ 1600 N·m/rad（输出端）**，换算到转子端约 0.019 N·m/rad。
   也就是说在 0.024 N·m 以内**输出端编码器一动不动、只有转子侧的弹性卷绕**，
   卷绕量 1.5e-5 rad 是转子编码器分辨率（6.6e-7 rad）的 20 倍，测得到。
2. **所以前面第一版测到的 0.5–1.7° 偏差不是柔度**，而是回差（见第三节）。
3. **静摩擦在"慢速蠕动"下只有约 0.028–0.032 N·m**，而快速破断需要 0.0365 N·m
   —— 破断点本身是速度/时间相关的（经典的 stick-slip）。做 sim2real 时如果策略是慢速
   精细动作，会先遇到 0.03 这一档。

---

## 三、M7 回差：1.06°（手册"S288 无需 backlash 模型"的说法不成立）

方法：对 3 个位置，每个位置**分别从下方和上方逼近**，每次 1.8 s 稳定后取后半段均值，
比较转子端推出的输出角与输出端编码器的差。稳定时 `|v| = 0.00000 rad/s`（真停稳）。

| 目标 | 从下方逼近的差 | 从上方逼近的差 | 回差 |
|------|--------------|--------------|------|
| 0° | −1.854° | −0.803° | **1.052°** |
| +10° | −2.316° | −1.301° | **1.014°** |
| −10° | −1.947° | −0.846° | **1.101°** |

* **回差 = 1.056° = 0.0184 rad**（三个位置 0.0177–0.0192，一致性 ±0.06°）。
* 两次独立跑的结果 1.042° vs 1.056°，**重复性 0.014°**。
* 两个编码器之间还有一个**恒定偏置 −1.40° … −1.81°**（随位置变化 0.4°，
  含输出端 13 bit 编码器自身的非线性）：这一项是标定偏置，**只有回差那一项是 sim 里的
  backlash**，两者必须分开报。

**这与手册第三节"输出端 13 bit 编码器消除了反冲、因此不需要 backlash 模型"直接冲突。**
实测 1.06° 的输出端滞回对足式机器人不是小量（足端定位、关节零位都在这个尺度上），
建议在 MuJoCo 里给关节加 `frictionloss` 或显式 backlash —— 至少在策略做双向精细动作时。

---

## 四、M6b 惯量：J_out ≈ 5e-4 kg·m²（关节空间）

> **更正**：本节初版写的是"机身自由时物理上测不出惯量，需要夹持机身"。夹持实验（用手按住）
> **否掉了这个结论** —— 按住前后 4.0e-4 vs 4.7e-4，在窗口散布之内完全没变，说明机身反作用
> 从来不在测量路径里。错在两处：把"未经验证的 1.2e-6 转子惯量"当成了事实；把 τ 与 α 不相关
> 读成了"机身滑转"（正确读法见下"方法学结论 2"）。

方法：

```bash
python3 bam_dynamics.py inertia --taus 0.05,0.06,0.07 --secs 0.6 --win 20 --vmax 1.5
```

`kp=kd=0` 施加纯力矩前馈；位置用二次拟合出 α（20 帧 = 3.2 ms 窗口）；`J = (τ_rep − F(v))/α`。
**`--vmax 1.5` 是关键**：0.12 N·m 的阶跃几毫秒内就把轴推到 13 rad/s，远超摩擦模型的拟合区间
（0.02–1.5 rad/s），那些窗口是在用外推的摩擦算 J，必须丢弃（第一版没做这件事）。

| τ 命令 | 窗口数 | J 中位 | 散布 |
|--------|-------|--------|------|
| 0.05 N·m | 11 | 3.4e-4 | 2.0e-4 – 5.2e-3 |
| 0.06 N·m | 9 | 6.8e-4 | 3.0e-4 – 6.6e-3 |
| 0.07 N·m | 5 | 5.8e-4 | 4.0e-4 – 9.0e-4 |
| 合并 | 27（正 25） | **4.7e-4** | 2.1e-4 – 6.6e-3 |

**取哪一档**：`J = (τ−F)/α`，α→0 的窗口数学上必然发散（实测 0.005–0.0066），所以均值没意义。
最可靠的是 **α 最大的窗口**（每个阶跃的第一窗：α = 53 / 74 / 96 rad/s²，惯性力矩 24–36 mN·m，
是摩擦模型残差 1 mN·m 的 10 倍以上）：J = **4.5 / 3.7 / 3.8 e-4**。对"惯性力矩 ≥ 10× 摩擦残差"
的 22 个窗口取中位 = **5.1e-4**。

**结论：J_out ≈ 4–6e-4 kg·m²（取 5e-4）**，换算到转子端 `J_rotor = J/RATIO² = 6.1e-9 kg·m²`。

### 为什么不是原先估计的 0.1

那个数建立在"转子惯量 1.2e-6 kg·m²"这个我随手假设、从未测量的前提上。反过来看：手册自己的
堵转数据（0.32 A → 转子 3.4e-4 N·m）给出 **kt ≈ 1e-3 N·m/A**，比常见小无刷低一个数量级 ——
这是"高转速、低力矩、小转子"电机的特征，转子惯量 6e-9（约 6 mm 直径转子）与之相符。
所以 5e-4 不是"偏小 200 倍"，而是**修正了一个错的前提**。

### 交叉验证

* 机身按住 vs 自由：4.0e-4 vs 4.7e-4 —— 在散布之内一致。
* 与 microduck 自己发布的参数同量级：XL330 的 `m6.json` `armature = 0.00157`（同为 288:1，
  关节空间），实测 5e-4 相差约 3 倍。**而这个数是测出来的，不是拟合出来的。**

### 两个方法学结论

1. **正弦法在 J 很小时不可用，瓶颈是相关模型误差而不是噪声**：0.25 rad @0.5 Hz 时惯性力矩只有
   `5e-4 × 1.53 = 0.8 mN·m`，而力矩残差 35–40 mN·m，信噪比 ~0.02。
   `selftest.py` 用同样的噪声水平（sd 40 mN·m）合成 J=0.1 的正弦数据能把 J 反演到 0.1%
   —— 说明**纯随机噪声不是问题，问题是残差与 α 相关**（跟踪误差 + 摩擦模型形状误差）。
   要用正弦法测 5e-4 量级需要约 10 倍激励；力矩阶跃法才是对的工具。
2. **τ 与 v 相关、与 α 不相关，是小 J 的必然结果，不是机身滑转的证据**：0.5 Hz 正弦里惯性项
   本来就只有 0.8 mN·m。这条当时被我读反了 —— `selftest.py` 里"用实测噪声反演 J"那条测试
   就是为这个判据留的。

---

## 五、汇总

| 量 | 值 | 单位 | 数据来源 | 状态 |
|----|-----|------|---------|------|
| command_delay（传输） | 0.48 | ms | M2 拟合（80 次跃变） | ✅ 可用 |
| 力矩一阶上升 τ | 0.54 | ms | M2 拟合 | ✅ 可用 |
| 力矩 10–90% 上升 | 1.19 | ms | M2 拟合 | ✅ 可用 |
| 传动链刚度（输出端） | ≥ 1600 | N·m/rad | M6b 柔度 | ✅ 下限 |
| 传动链刚度（转子端） | ≈ 0.019 | N·m/rad | M6b 柔度 | ✅ 约值 |
| 慢速蠕动起点 | 0.028–0.032 | N·m | M6b | ✅ |
| 快速破断（静摩擦） | 0.0365 | N·m | M4 力矩斜坡 | ✅ |
| 回差（输出端） | 0.0184 (1.056°) | rad | M7 双向逼近 ×3 | ✅ |
| 编码器间恒定偏置 | ≈ −0.029 (−1.63°) | rad | M7 | ✅ 标定值 |
| **关节等效惯量 J_out** | **5e-4 (4–6e-4)** | kg·m² | M6b 力矩阶跃，α 最大窗口 | ✅ 空载、本台架 |
| 转子等效惯量 J_rotor | 6.1e-9 | kg·m² | = J_out / RATIO² | ✅ 同上 |

## 六、仍未做

| 项 | 为什么没做 |
|----|-----------|
| 电压敏感性（M3） | 只有 12 V 电源；母线读数分辨率 0.5 V 太粗 |
| 最大力矩 / 0.32 A 限流 | 需要砝码或堵转装置（本记录全部是空载） |
| 温度相关性 | 壳体 55→60 ℃ 期间摩擦力矩漂移在 ±0.002 N·m 内，暂不给模型 |
| 多圈/多次上电一致性 | 单圈绝对编码器上电清零，未测 |
| 单元间个体差异 | 手上只有一颗 S288，所有数字都是 ID 0 这一颗的 |
